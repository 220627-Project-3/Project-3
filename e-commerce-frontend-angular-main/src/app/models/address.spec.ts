import { Address } from './address';

describe('Address', () => {
  it('should create an instance', () => {
    expect(new Address("Bob","Bob","Bob","Bob","Bob","Bob","Bob","Bob")).toBeTruthy();
  });
});
